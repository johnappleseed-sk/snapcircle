class RealtimeConfig {
  static const Duration notificationPollInterval = Duration(seconds: 30);
  static const Duration feedStatusPollInterval = Duration(seconds: 45);
  static const Duration commentsStatusPollInterval = Duration(seconds: 30);
}
