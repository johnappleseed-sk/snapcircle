import '../constants/app_sizes.dart';

class AppRadius {
  const AppRadius._();

  static const double xs = 8;
  static const double sm = AppSizes.radiusSmall;
  static const double md = AppSizes.radiusMedium;
  static const double lg = AppSizes.radiusLarge;
  static const double xl = AppSizes.radiusXL;
  static const double pill = 999;
}
